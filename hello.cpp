#include <iostream>
/* this basically copy pastes the contents of the iostream
 * C++ file. This allows us to do the basic functionality of 
 * C++ programs */
using std::cout;
/* NOTE: cout has a first name (cout_ and a last name
 * which is (std). Without this "using" statement we
 * would have to say "std:cout" instead of just "cout". 
 * this is the starting point of the program: */

int main() {
	cout << "HELLO MY NAME IS NAMAN\n";
	return 0;
}

